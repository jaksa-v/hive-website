---
import Carousel from "../components/Carousel";
import { items } from "../data/programsData";
---

<section
  id="programs"
  class="h-[calc(100vh-56px)] max-w-screen-lg mx-auto px-[30px] md:px-[0px] flex flex-col justify-center"
>
  <div class="flex flex-col gap-y-[30px]">
    <div>
      <h1
        class="text-white font-semibold text-[28px] md:text-[36px] lg:text-[48px] leading-[1.4]"
      >
        Najpopularniji Programi
      </h1>
      <p class="text-white mt-[10px]">
        Pogledajte naše najtraženije tim bilding aktivnosti.
      </p>
    </div>
    <div id="carousel-wrapper">
      <Carousel
        client:only="react"
        slides={items.map((item) => ({ type: "program", data: item }))}
      />
    </div>
  </div>
</section>
